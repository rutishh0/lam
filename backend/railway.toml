[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"

[env]
PYTHON_VERSION = "3.11"
PORT = "8000"
PYTHONPATH = "." 